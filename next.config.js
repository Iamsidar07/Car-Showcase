/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "cdn.imagin.studio",
            "avatars.githubusercontent.com"
        ]
    }
}

module.exports = nextConfig
